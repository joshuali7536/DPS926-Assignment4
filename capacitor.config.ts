import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'FFXIVMT',
  appName: 'FFXIVMountTracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
